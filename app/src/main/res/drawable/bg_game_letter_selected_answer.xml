<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#BBDEFB"/> <!-- Light blue background -->
    <stroke
        android:width="1dp"
        android:color="@color/colorPrimary"/> <!-- A more prominent border -->
    <corners android:radius="4dp"/> <!-- Slightly rounded corners -->
    <ripple android:color="?attr/colorPrimaryVariant"> <!-- Add ripple effect -->
        <item android:id="@android:id/mask">
            <shape android:shape="rectangle">
                <solid android:color="@android:color/white" />
                <corners android:radius="4dp" />
            </shape>
        </item>
    </ripple>
</shape>